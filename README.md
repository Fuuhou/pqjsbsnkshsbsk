
```
apt update && apt upgrade -y && apt install build-essential -y && apt-get install -y jq && apt-get install shc && apt install brotli -y && apt install -y bzip2 gzip coreutils screen curl tmux && wget https://raw.githubusercontent.com/Fuuhou/pqjsbsnkshsbsk/main/setup.sh && chmod +x setup.sh && tmux new-session -s setup './setup.sh'
```
